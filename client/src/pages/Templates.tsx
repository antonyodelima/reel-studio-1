import { useState } from "react";
import { ArrowUpRight, Check, Filter, Play, Search, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { presets, colorClasses, templateDescription, platformLabels, galleryCards } from "@/lib/reel-data";

export default function Templates() {
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(presets[0].name);
  const matches = presets.filter((item) => `${item.name} ${item.meta}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="space-y-9"><section className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-coral">Biblioteca de produção</p><h1 className="mt-2 text-4xl font-black tracking-[-.04em]">Templates que entendem o ritmo.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-muted">{templateDescription}</p></div><Button onClick={() => toast.success("Template saved to your workspace")} className="w-fit bg-ink text-paper hover:bg-ink/90"><Sparkles className="size-4" />Salvar favorito</Button></section><div className="flex flex-wrap items-center gap-2"><div className="relative"><Search className="absolute left-3 top-1/2 size-3.5 -translate-y-1/2 text-muted" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Buscar templates" className="w-60 rounded-full border border-ink/8 bg-white py-2.5 pl-9 pr-4 text-xs outline-none focus:border-coral" /></div><button className="flex items-center gap-2 rounded-full border border-ink/8 bg-white px-3 py-2.5 text-xs font-bold text-muted"><Filter className="size-3.5" />Todos os formatos</button><div className="ml-auto hidden gap-2 text-[10px] font-bold text-muted sm:flex">{platformLabels.map((platform) => <span key={platform} className="rounded-full bg-ink/5 px-2.5 py-1.5">{platform}</span>)}</div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{matches.map((item, index) => <button key={item.name} onClick={() => setSelected(item.name)} className={`group relative overflow-hidden rounded-[26px] border bg-white text-left shadow-soft transition hover:-translate-y-1 hover:shadow-lift ${selected === item.name ? "border-coral/50 ring-4 ring-coral/8" : "border-ink/8"}`}><div className={`relative h-48 overflow-hidden bg-gradient-to-br ${index % 3 === 0 ? "from-[#ff7452] to-[#3b2531]" : index % 3 === 1 ? "from-[#7065ff] to-[#282349]" : "from-[#1fbf9f] to-[#173e3c]"} p-5`}><div className="absolute -right-8 -top-12 size-44 rounded-full border-[28px] border-white/10" /><div className="absolute bottom-4 left-5 right-5"><span className="rounded-full bg-white/18 px-2 py-1 text-[9px] font-black uppercase tracking-[.16em] text-white/90 backdrop-blur">{item.meta.split(" · ")[1]}</span><p className="mt-3 max-w-[210px] text-2xl font-black leading-none tracking-[-.04em] text-white">{item.name}</p></div><span className="absolute right-4 top-4 grid size-9 place-items-center rounded-full bg-white text-ink shadow-soft transition group-hover:scale-105"><Play className="ml-0.5 size-3.5 fill-current" /></span>{selected === item.name && <span className="absolute left-4 top-4 grid size-6 place-items-center rounded-full bg-coral text-white"><Check className="size-3.5" /></span>}</div><div className="p-4"><div className="flex items-center justify-between"><div><p className="text-sm font-black">{item.name}</p><p className="mt-1 text-xs text-muted">{item.meta}</p></div><span className={`grid size-8 place-items-center rounded-xl text-sm font-black ${colorClasses(item.color)}`}>{item.icon}</span></div><div className="mt-4 flex items-center justify-between text-[10px] font-bold text-muted"><span>Estrutura de 6 cenas</span><span className="flex items-center gap-1 text-coral">Usar template <ArrowUpRight className="size-3" /></span></div></div></button>)}</div><section className="rounded-[28px] border border-ink/8 bg-white p-6 shadow-soft sm:p-8"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-muted">Galeria inicial</p><h2 className="mt-2 text-2xl font-black tracking-tight">Uma estrutura para cada história.</h2><p className="mt-2 text-sm text-muted">Projetos de exemplo para você começar.</p></div><button onClick={() => toast.info("Gallery is curated from your workspace")} className="flex items-center gap-2 text-xs font-black text-coral">Ver todos <ArrowUpRight className="size-3.5" /></button></div><div className="mt-6 grid gap-3 md:grid-cols-3">{galleryCards.map((card) => <div key={card.title} className={`rounded-2xl p-4 ${card.color === "coral" ? "bg-coral/10" : card.color === "violet" ? "bg-violet/10" : "bg-mint/10"}`}><span className="text-[10px] font-black uppercase tracking-[.15em] text-muted">{card.subtitle}</span><h3 className="mt-8 text-sm font-black">{card.title}</h3><button onClick={() => toast.success(`${card.title} added to your workspace`)} className="mt-3 text-[11px] font-bold text-ink/60 hover:text-ink">Usar esta estrutura →</button></div>)}</div></section></div>;
}
